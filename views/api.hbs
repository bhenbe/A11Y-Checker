[
    {{#each result.issues}}{
    "type": "{{this.type}}"
    "typeCode": {{this.typeCode}}
    "selector": "{{this.selector}}"
    "message": "{{this.message}}"
    "context": "{{this.context}}"
    }{{/each}}
]